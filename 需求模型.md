# Sub2API 用户账户与模型网关需求模型

状态：阶段 A 的 Host runtime、fixture double、定向竞态测试和 Cordis façade 已落地；阶段 B 的动态模型 route、共享 pi-ai 流式适配、模型/用量缓存和错误映射已落地；阶段 C 的 Remote namespace、事件转发、浏览器设置区和本地化文案已落地。真实目标部署证据尚未提供，Responses 仍保持关闭。

输入：[`2026-09-16-sub2api-user-account-model-gateway.zh.revised.md`](2026-09-16-sub2api-user-account-model-gateway.zh.revised.md)、当前 Harness `master`（`da03ba5`）、Sub2API 锁定提交 `881f3202694c6bc932446931a30c27d9675178b9` 的公开源码观察。

本文记录需求模型、第一轮技术设计和当前实现边界。计划书中的命令、验收清单和“必须创建”表述不是本文件之外的授权；实现仍需按阶段和用户确认推进。

## 目标

Harness 提供一个独立的 Sub2API 第一方账户能力，使用户能在 Harness 中注册或登录 Sub2API，安全托管该用户的 Sub2API API Key，通过同一账户服务发现可用模型、读取账户状态，并在余额不足时获得可恢复的充值入口。

V1 成功标准：账户凭据不进入浏览器、Remote、设置文档、日志或会话事件；模型请求复用现有 `llm-pi-ai` OpenAI 兼容路径；账户状态、模型发现、错误和并发行为可以通过协议 fixture 重放和验证。

## 范围

### 包含

- 一个独立的 `sub2api` Host capability，集中拥有账户会话、持久凭据、协议 profile、HTTP 请求、模型发现、用量和错误映射。
- 单个 Harness profile 同时只有一个 active Sub2API deployment 和一个 active Sub2API user account。
- 内存中的 access token、凭据 provider 中的 refresh token（仅当上游正式提供并通过 fixture 验证时）以及 Harness 自己管理的 API Key。
- 登录、注册、当前用户读取、确定性 API Key 复用或创建、状态刷新和受控的模型发现。
- 生产 HTTPS-only、重定向逐跳校验、公开充值 URL 校验、响应体上限、取消和超时。
- 阶段 B 通过 Host runtime resolver 将托管 API Key 接入保留的 `sub2api` LLM route，并复用 `llm-pi-ai` 的 Chat Completions 和 SSE 实现。
- 阶段 C 通过现有 `settings.section` slot、Remote、locale 和 open-url 能力提供账户 UI。

### 不包含

- Sub2API Admin API、Admin API Key、上游账号池管理、自有支付订单、支付签名、webhook 和支付账本。
- 把 Sub2API 登录会话用于 Host RPC 授权。
- 将密码、access token、refresh token、API Key secret、完整认证响应或完整模型响应放入设置、Remote、日志、快照、遥测或 URL。
- 根据模型名称推断 tools、vision、reasoning、Responses 或上下文容量。
- 没有真实协议 fixture 时启用 Responses。

## 角色

| 角色 | 可以做什么 | 不可以做什么 |
|---|---|---|
| Harness Host | 保持账户会话、读写凭据、调用 Sub2API 用户 API、解析响应和管理 session generation | 不持久化密码，不使用 Admin API，不把 secret 投影到 Client |
| 浏览器 Client | 提交用户表单、显示脱敏状态、触发账户动作和刷新请求 | 不读取或保存 token、API Key 或原始上游响应 |
| Headless/CLI | 使用已有账户状态发送模型请求，缺少账户时返回登录要求 | 不假装提供交互式登录，不在无授权时发起模型网络请求 |
| Sub2API 用户 API | 验证用户身份、发放会话、管理用户 API Key、提供账户/用量/模型数据 | 不属于 Harness 的本地授权体系 |
| `llm-pi-ai` | 复用 OpenAI 兼容协议转换、SSE、工具调用、用量、重试和超时处理 | 不拥有 Sub2API 账户或凭据记录 |

## 输入

| 输入 | 来源 | 约束 |
|---|---|---|
| deployment base URL | Host 配置 | 生产只允许 HTTPS；无 userinfo；连接前解析并拒绝本机、私有、链路本地、组播、未指定和保留地址 |
| recharge URL | Host 配置或已核验服务端字段 | 只有通过 scheme、host 和 redirect 策略的 URL 才能交给 open-url |
| email/password | Host 交互流程 | 只在一次认证请求生命周期内存在；密码不进入持久化、事件或诊断 |
| username | 注册流程 | 是否是锁定基线的必要字段尚未确认；不得仅按计划书硬编码为必填 |
| 2FA 临时凭据和代码 | Host 交互流程 | 仅当目标部署返回明确的 2FA 状态且 V1 支持时存在 |
| access token | Sub2API 登录/刷新响应 | 只保存在进程内存 |
| refresh token | Sub2API 刷新响应 | 只有正式接口和字段经 fixture 验证后才进入 credential provider |
| managed API Key | Sub2API 用户 API | 由 Harness 创建或确定性复用；secret 只进入 credential provider 和当前请求快照 |
| 模型、用量、余额响应 | Sub2API 用户或网关 API | 先限制完整响应大小，再解析 direct 或 `{ data: ... }` envelope |

## 输出

- `Sub2apiStateView`：认证状态、脱敏账户摘要、余额/用量摘要、模型发现状态、stale 标记和稳定错误码。
- `Sub2apiGrantRecordV1`：deployment 绑定、账户摘要、托管 API Key 元数据和受条件约束的 refresh token。
- `Sub2apiModelDescriptor[]`：模型 id、非敏感显示字段、fixture 已验证的能力和来源；未知能力保持 `unknown`。
- `Sub2apiError`：稳定产品错误码、HTTP 状态类别、是否可重试、恢复动作和可选 `Retry-After`，不携带原始响应正文。
- `llm-pi-ai` 的 provider route：仅在当前会话和托管 API Key 快照都有效时服务模型请求。
- Remote/UI 的脱敏投影：不含 access token、refresh token、API Key、Authorization、Cookie、密码、原始认证响应或完整上游响应。

## 状态与流转

| 当前状态 | 事件 | 下一状态 | 提交条件 |
|---|---|---|---|
| `signed-out` | 开始登录/注册 | `authenticating` | 认证输入已校验，尚未发送请求时不改变凭据 |
| `authenticating` | 目标要求 2FA | `two-factor-required` | 只保存短期流程上下文，不保存密码 |
| `two-factor-required` | 2FA 成功 | `authenticated` 或 `key-required` | token 与 `/me` 账户一致，随后解析 managed key |
| `authenticating` | 登录成功 | `authenticated` 或 `key-required` | `/me` 成功确认 token 与账户一致 |
| `authenticated` | access token 临近过期且存在正式 refresh | `refreshing` | refresh singleflight 已建立 |
| `refreshing` | refresh 成功 | `authenticated` | 原子替换 runtime token；轮换的 refresh token 原子写入记录 |
| `refreshing` | refresh 失败且不可恢复 | `reauth-required` | 当前 token 失效，停止重复 refresh |
| `authenticated` | 没有有效 managed key | `key-required` | key reconciliation 尚未成功 |
| `key-required` | key 创建或唯一复用成功 | `authenticated` | secret 已在后续网络请求前完成安全持久化 |
| `authenticated` | 模型请求返回余额不足 | `insufficient-balance` | 不自动重试，暴露可恢复充值动作 |
| 任意非 `signing-out` 状态 | 退出登录 | `signing-out` | 先推进 generation，令 logout 获胜 |
| `signing-out` | 本地清理完成 | `signed-out` | 清除 token、记录、缓存和进行中的账户状态；保留匿名身份与浏览器认证 |

每次成功登录、凭据替换、deployment 变化和退出登录都递增 `sessionGeneration`。异步请求在发送前捕获 generation；响应提交前若 generation 已变化，则丢弃该结果。

## 业务规则

| 编号 | 规则 | 证据 |
|---|---|---|
| R1 | Sub2API 账户会话与 conversation `SessionId`、匿名遥测身份和浏览器认证 cookie 分离。 | E1：计划书第 1、5 节 |
| R2 | access token 只保存在进程内存；密码绝不持久化。 | E1：计划书第 2、6 节 |
| R3 | refresh token 只有在目标部署正式提供且 fixture 证明后才持久化。 | E1：计划书第 7 节 |
| R4 | API Key secret 只能由 Host credential provider 管理；模型发送必须同时需要有效账户会话和 API Key 快照。 | E1：计划书第 6、11 节 |
| R5 | logout 默认不删除 Sub2API 服务端 managed key，但必须清除本地 API Key、账户记录和缓存。 | E1：计划书第 8 节 |
| R6 | `/v1/models` 只证明模型可发现，不证明 tools、vision、reasoning 或 Responses 能力。 | E1：计划书第 11 节 |
| R7 | SSE 已产生输出后断流不得自动整体重放。 | E1：计划书第 10、20 节 |
| R8 | Remote/UI 只能消费脱敏投影；Sub2API 登录身份不授权任意 Host RPC。 | E1：计划书第 13、15 节 |
| R9 | 认证 refresh、managed key reconciliation、账户刷新和模型刷新使用 singleflight；旧 generation 的结果不得覆盖新状态。 | E1：计划书第 7、12 节 |
| R10 | 生产请求只使用 HTTPS；每个 redirect 目标重新执行完整 host 策略。 | E1：计划书第 9 节 |
| R11 | 当前 Harness 已有凭据 record 的写入入口是 `CredentialProvider.modifyRecord`，记录内容由 owner 负责解释。 | E2：`packages/credentials/credentials/src/index.ts:247`、`packages/credentials/credentials/src/types.ts:47` |
| R12 | `llm-pi-ai` 当前通过 adapter 的 `resolveApiKey` 在请求前按 route 解析凭据，并通过 `ctx.llm.registerAdapter` 注册 provider。 | E2：`packages/llm/llm-pi-ai/src/index.ts:140`、`packages/llm/llm/src/index.ts:385` |
| R13 | Settings UI 当前通过 `settings.section` slot 注册页面，并通过 `ctx.remote` 和 locale dictionary 获取 Host 状态和文案。 | E2：`packages/client/ui-settings-models/src/client/index.ts:74`、`packages/client/ui-settings-models/src/client/index.ts:127` |
| R14 | 锁定 Sub2API 基线的前端登录路径为 `/api/v1/auth/login`，注册路径为 `/api/v1/auth/register`，当前用户路径为 `/api/v1/auth/me`；这与计划书中的示例路径不同。 | E2：Sub2API `frontend/src/api/auth.ts:121`、`:161`、`:180` |
| R15 | 锁定基线的注册类型包含 `email`、`password` 及可选验证码字段，没有 `username` 字段；Harness 不应在没有目标部署证据时把 `username` 写成必填。 | E2：Sub2API `frontend/src/types/index.ts:128` |
| R16 | 锁定基线的用户 API Key 路径为 `/api/v1/keys`；模型网关认证中间件优先解析 `Authorization: Bearer`，随后支持 `x-api-key`，并拒绝 query 中的 API Key。 | E2：Sub2API `frontend/src/api/keys.ts:15`、`:56`；`backend/internal/server/middleware/api_key_auth.go:44`、`:52`、`:63` |
| R17 | 锁定基线的用量 dashboard 路径为 `/api/v1/usage/dashboard/stats`、`/trend`、`/models` 等；计划书中的 `/api/usage/dashboard` 不是已确认的完整路径。 | E2：Sub2API `frontend/src/api/usage.ts:244`、`:253`、`:276` |
| R18 | 锁定基线的 refresh 请求使用 `/api/v1/auth/refresh` 和 `refresh_token` 字段，响应包含 `access_token`、`refresh_token`、`expires_in`、`token_type`。 | E2：Sub2API `frontend/src/api/tokenRefresh.ts:135`、`:154` |

## 异常

| 场景 | 预期行为 | 证据 |
|---|---|---|
| 无有效账户会话 | 在任何模型网络请求前返回 `SUB2API_NOT_AUTHENTICATED` 或 `SUB2API_REAUTH_REQUIRED`。 | E1：计划书第 11、14 节 |
| token 401 | 使当前内存 token 失效；有正式 refresh 时最多 refresh + replay 一次，否则进入重新认证。 | E1：计划书第 7、10 节 |
| key 无效或已撤销 | 进入 `key-required`，最多执行一次 reconciliation，不得无限创建 key。 | E1：计划书第 8 节 |
| 余额不足/402 | 不重试，进入 `insufficient-balance` 并暴露充值恢复动作。 | E1：计划书第 10、15 节 |
| 403 | 不进入认证循环；按目标协议区分权限、key、账户或分组不可用。 | E1：计划书第 10 节 |
| 429 | 保留 `Retry-After`，交由 provider 重试策略或 UI 恢复。 | E1：计划书第 10 节 |
| timeout、取消、5xx | 映射为稳定错误；遵守操作类型的受控重试，不让 logout 等待无界网络操作。 | E1：计划书第 9、10 节 |
| 响应格式错误或超大响应 | 在解析前返回 `SUB2API_BAD_RESPONSE`，不把原始正文写入错误、日志或 Remote。 | E1：计划书第 9、17 节 |
| URL 指向私有/本机地址 | 在 DNS 解析和连接前拒绝；redirect 目标逐跳复查。 | E1：计划书第 9 节 |
| 旧请求晚于 logout 返回 | generation 不匹配，丢弃结果，不覆盖新状态。 | E1：计划书第 7、12 节 |
| 多个同名 managed key | fail closed，不猜测，不自动使用普通用户 key。 | E1：计划书第 8 节 |

## 依赖

| 依赖 | 责任 | 阶段 |
|---|---|---|
| `@deepseek-ai/dsh-credentials` | GrantRecord 的持久读写和 record update 事件 | A |
| `@deepseek-ai/dsh-authorization` | 有交互组合中的登录、注册和 2FA 授权流程 | A/C |
| `@deepseek-ai/dsh-llm` | 稳定 LLM 错误、provider 注册和 retry 语义 | B |
| `@deepseek-ai/dsh-llm-pi-ai` | OpenAI 兼容转换、SSE、tools、usage 和模型请求 | B |
| `@deepseek-ai/dsh-typert-protocol` / Remote gateway | Host 方法到 Client 的脱敏投影 | C |
| `@deepseek-ai/dsh-client-ui-slots` / locale / open-url | Settings 区域、文案、充值页面打开 | C |
| 锁定 Sub2API 基线 | 端点、字段、状态码、认证头、响应 envelope、SSE 和用量行为 | A/B |
| 目标实际部署 | 与锁定基线不同的部署配置和功能开关 | A/B/C |
| HTTP protocol fixtures | 在没有真实密钥时证明协议行为和安全失败路径 | A/B |

## 第一轮技术设计

### 包边界

阶段 A/B 当前由实验库 `packages/experimental/sub2api` 负责 Sub2API 账户协议、Host runtime 和可选模型 route；浏览器投影位于 `packages/client/ui-settings-sub2api`。已实现的部分是：

- `types.ts`：wire-safe 状态、错误摘要、账户摘要、协议 profile 和 GrantRecord V1 类型。
- `profile.ts`：版本化 protocol profile、endpoint path 解析、响应 envelope 选择和部署 URL 校验。
- `record.ts`：GrantRecord V1 校验、Secret 标记和脱敏投影。
- `state.ts`：账户状态机、稳定状态错误和单调递增的 generation guard。
- `errors.ts`：稳定错误分类和 Remote/UI 可用的错误摘要。
- `http.ts`：注入目标校验器的有界 HTTP 请求、认证 header、同源 redirect、响应体上限、取消/超时和状态映射。
- `protocol.ts`：有界 JSON 解析和 direct/data envelope 选择，不推断业务字段。
- `service.ts`：Host runtime，负责登录、注册、2FA、refresh、账户/模型/用量缓存、managed API Key reconciliation、充值 URL 校验和 generation 保护。
- `fixtures.ts`：版本化业务 fixture 解析/重放、可记录请求的 fetch double、JSON response helper 和内存凭据存储，用于协议与竞态测试。
- `compatibility.ts`：锁定基线、目标部署与合成 fixture 分离的 P0/P1 兼容性矩阵解析器。
- `llm.ts`：保留的 `sub2api` provider route；通过 `llm-pi-ai` 复用 OpenAI Chat Completions、SSE、usage、超时和重试链路，并按模型元数据 fail-closed。
- `remote.ts`：`sub2api` Remote namespace，仅输出账户、模型、用量、充值 URL 和稳定错误摘要。
- `tests/fixtures/account-v1.json`：只含合成值的登录、当前用户、managed key、模型和用量重放样本；不代表真实目标部署。
- `tests/fixtures/gateway-v1.json`：模型发现、非流式 Chat Completions、SSE、usage 和网关 header 的合成重放样本。
- `tests/fixtures/compatibility-matrix-v1.json`：19 个 P0 与 P1 可选能力的证据状态样本，目标部署当前为 `not-provided`。
- `cordis.ts`：把 runtime 挂载为 `ctx.sub2api`，并复用已安装的 `ctx.credentials`。
- `packages/api/remotes`：挂载 `sub2api` Remote client、Host 类型与 `sub2api/state-changed` 事件转发。
- `packages/client/ui-settings-sub2api`：注册账户设置区，提供登录、注册、2FA、退出、余额/用量、模型刷新和充值页面打开。

尚未关闭的部分是：

- 可审计的锁定基线/目标部署 fixtures：当前已有版本化合成协议样本，但还没有锁定基线或目标部署的脱敏样本。
- 锁定基线与目标部署的真实脱敏协议证据：当前矩阵和 gateway fixture 都没有把 `targetStatus` 标为 `verified`。
- 真实部署的 DNS/连接验证、实际错误码和真实充值跳转：仓库内已完成 fail-closed 策略与合成夹具，但不能替代外部环境证据。
- Responses 独立 fixture、完整事件/usage/终止/取消契约：feature flag 保持关闭。
- tools、vision、reasoning 等模型能力的目标部署验证：模型列表缺少明确 metadata 时继续使用 `unknown`。

`http.ts` 不自行解析 DNS 或固定连接地址；生产组合必须注入负责解析、校验并固定实际连接目标的 validator。这样请求策略可以先由 fixture 验证，而不会把未验证的部署字段写入默认 profile。

阶段 B 已在阶段 A 的 provider/key/profile 合同之上扩展 `llm-pi-ai` 的 Host-only route registration、runtime resolver、动态模型目录、Chat Completions/SSE 和 usage/error 映射。阶段 C 已增加独立 Client package、Remote 脱敏投影和账户 UI；账户 UI 不直接读取 Host record。

### Host 服务最小接口

```ts
interface Sub2apiRuntime {
  state(): Sub2apiStateView
  register(input: Sub2apiRegisterInput, signal?: AbortSignal): Promise<void>
  login(input: Sub2apiLoginInput, signal?: AbortSignal): Promise<void>
  submit2FA(input: Sub2apiTwoFactorInput, signal?: AbortSignal): Promise<void>
  logout(): Promise<void>
  refreshAccount(signal?: AbortSignal): Promise<Sub2apiAccountSnapshot>
  refreshModels(signal?: AbortSignal): Promise<readonly Sub2apiModelDescriptor[]>
  getUsage(signal?: AbortSignal): Promise<Sub2apiUsageSnapshot>
  getRechargeUrl(signal?: AbortSignal): Promise<string | undefined>
  resolveGatewayCredential(signal?: AbortSignal): Promise<Sub2apiGatewayCredential>
}
```

`Sub2apiRuntime` 的公共方法返回脱敏值或已约束的请求快照；`resolveGatewayCredential` 只能被 Host provider 调用，不能出现在 Client-safe 类型和 Remote descriptor 中。具体方法名和 Typert `@Remote` 投影在阶段 A 的协议字段关闭后定稿。

### 凭据记录

```ts
interface Sub2apiGrantRecordV1 {
  version: 1
  deployment: { origin: string; fingerprint: string }
  account: { userId: string; email?: string; displayName?: string }
  auth: { refreshToken?: Secret; refreshTokenType?: string; lastRefreshAt?: number }
  apiKey: { id?: string; name: string; secret: Secret; fingerprint?: string; createdAt?: number }
  generation: number
  updatedAt: number
}
```

记录中的 `Secret` 代表 owner 内部的 secret 类型；它不能作为 Client-safe JSON、Remote 结果、设置字段或日志字段出现。若基线只在创建响应返回 API Key secret，必须在任何后续请求前完成 record 写入，否则安全失败并保持 `key-required`。

### 阶段顺序

1. 阶段 A：完成基线/部署矩阵、profile、record、认证状态、HTTP/SSRF 策略、错误映射和 fixture。
2. 阶段 B：接入保留 provider、managed key resolver、动态模型、Chat Completions/SSE、用量和共享组合。
3. 阶段 C：接入 Settings account section、Remote 脱敏投影、locale、充值 URL 和返回刷新。
4. 阶段 D：只有 Responses 的真实事件、工具、用量、终止、取消和错误 fixture 全部通过后才启用。

当前阶段 A/B/C 的代码与文档设计已完成；后续工作是用锁定基线和实际目标部署证据更新矩阵，再决定是否打开相应的目标能力开关。

## 兼容性矩阵初稿

| 能力 | 锁定基线观察 | 目标部署 | 设计处理 |
|---|---|---|---|
| 注册 | `/api/v1/auth/register`；`email/password`，可选验证码；`username` 未见于基线类型 | 待核验 | profile 字段化；关闭前不把 username 写成必填 |
| 登录 | `/api/v1/auth/login`；`email/password` | 待核验 | profile 字段化；成功后强制 `/me` 一致性确认 |
| 当前用户 | `/api/v1/auth/me`；计划书 `/api/user/me` 未被基线确认 | 待核验 | 不能硬编码计划书示例路径 |
| 2FA | `/api/v1/auth/login/2fa` 及 `requires_2fa` 类型出现在基线源码 | 待核验 | 先识别状态；是否支持由 P0 决定 |
| refresh | `/api/v1/auth/refresh`；refresh token rotation 字段存在于基线前端类型 | 待核验 | 仅按正式响应启用；singleflight |
| 用户 API Key | `/api/v1/keys` 的 list/create/get/delete 客户端存在；secret 返回时机需 fixture 确认 | 待核验 | 只管理 deterministic name 的 Harness key |
| Gateway header | `Authorization: Bearer` 优先，支持 `x-api-key`；query key 被拒绝 | 待核验 | 以 profile 选择一个已验证头，不同时猜测 |
| Models | 计划书要求 `/v1/models`；基线 API 源码未在本轮完整定位 | 待核验 | 阶段 B 前用 gateway fixture 锁定 |
| Chat Completions/SSE | 计划书要求复用现有 pi-ai 路径 | 待核验 | 不复制 adapter；fixture 证明分帧和错误 |
| Usage/Balance | 基线 dashboard 路径为 `/api/v1/usage/dashboard/stats`、`/trend`、`/models`；余额字段在用户类型有 `balance` | 待核验 | 账户与用量分开解析；保留 `asOf/stale` |
| Recharge URL | 未从锁定基线确认 | 待核验 | 仅配置或服务端字段经过 URL policy 后开放 |
| Responses | 未验证 | 待核验 | 默认关闭，独立 feature flag |

## 未决问题

以下问题会改变协议、权限、安全或交付范围，必须在对应阶段前关闭；本轮不硬猜。

### P0

1. 目标实际部署的 origin、版本和配置是什么？它是否就是 `Wei-Shaw/sub2api` 的锁定提交 `881f3202694c6bc932446931a30c27d9675178b9` 对应部署？
2. 目标部署的注册、登录和当前用户准确 endpoint、字段、验证码要求和 envelope 是什么？计划书中的 `/api/user/me` 是否应改为基线观察到的 `/api/v1/auth/me`？
3. 注册是否需要 `username`？若目标部署不需要，Harness 是否只提交 `email/password`？
4. 目标部署是否返回正式 refresh token？refresh 是否轮换？失效时的状态码和错误码是什么？
5. 2FA 是否启用？V1 是完成 2FA 交互，还是在 `requires_2fa` 时显式返回 `SUB2API_2FA_UNSUPPORTED`？
6. 用户 API Key list/create/get 的准确响应字段是什么？secret 是否只在 create 返回？是否能按 id 查询当前用户归属和 active 状态？
7. Gateway API Key 在目标部署应使用 `Authorization: Bearer` 还是 `x-api-key`？模型 API 是否有额外 path 或 header 要求？
8. `/v1/models` 是否按用户、API Key、group 或 subscription 过滤？返回是标准 OpenAI `data` 列表还是其他 envelope？
9. Usage/Balance 的准确 endpoint、字段、币种、精度和时间口径是什么？402 是否稳定表示余额不足？
10. Recharge URL 从配置还是服务端获取？允许的 scheme、origin 和 redirect 规则是什么？
11. 生产是否完全禁止 HTTP？开发环境允许的明确 host allowlist 是什么？

### P1

1. 是否确认 V1 只支持单 deployment + 单 active account？
2. managed key 名称是否允许包含 installation fingerprint？
3. logout 是否确认不 revoke 服务端 managed key？是否需要后续显式“退出并撤销此设备 key”？
4. API Key disabled 时是复用、恢复，还是新建？
5. `/me`、models、usage 的最终 TTL 和 manual refresh 规则是什么？
6. 是否允许系统代理？若允许，代理目标和凭据如何纳入同一安全策略？
7. feature flag 存放在哪个现有配置面？

## 下一步

阶段 A/B/C 的 profile、record、脱敏、状态流转、HTTP 请求策略、envelope 解析、Host runtime、Cordis façade、动态 provider、Remote/UI 投影、版本化合成 fixture 和关键竞态已经有定向测试。下一安全动作是取得目标部署的脱敏协议证据或可重放 fixture，完成 P0-1 至 P0-11 中至少影响网络 provider 的项目。没有这些信息，不应启用 Responses，也不应把合成 fixture 的状态升级为目标部署证据。
